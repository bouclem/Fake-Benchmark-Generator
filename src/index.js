const { generateChart } = require('./generateChart');
const { generateScatterChart } = require('./scatterChart');
const { generateColors } = require('./colors');

module.exports = { generateChart, generateScatterChart, generateColors };
