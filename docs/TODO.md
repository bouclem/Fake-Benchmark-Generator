# TODO.md

- [ ] Optional PNG output (pure-JS encoder + bitmap font, or optional peer dep on sharp)
- [ ] Optional CLI (`bin/cli.js`) — decided against for v1.0.0, revisit if asked
- [ ] Horizontal bar variant for long model names
- [ ] Label-collision handling on the scatter chart
- [ ] Configurable theme (light/dark) — currently dark only
- [ ] Publish to npm (`npm publish`) — needs the user's registry auth
